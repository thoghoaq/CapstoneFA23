import 'package:design_kit/material.dart';
import 'package:flutter/material.dart';

class DBlur extends StatelessWidget {
  const DBlur({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: DColors.black.withOpacity(0.5),
    );
  }
}
